<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wm.repository.DishRepository">
    <select id="findAllDishes" resultType="com.wm.entity.DishEntity">
       SELECT
            id
            , name
            , description
            , category_id
            , price as original_price
            , price * coalesce(td.percent, 1) final_price
            , image
            , tags 
        FROM
            public.m_category
        ORDER BY
            display_order
    </select>
</mapper>