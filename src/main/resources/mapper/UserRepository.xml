<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wm.mapper.UserRepository">
    <select id="selectUserInfo" resultType="com.wm.entity.UserEntity">
       select
		    mu.user_name
		    ,mu.user_type
		    ,mu.last_login_time
		    ,mp.permission
		from
		    public.m_user mu
		    inner join (
		        select 
		            user_type 
		            ,string_agg(menu_key, ',') as permission
		        from
		            public.m_permission
		        group by
		            user_type
		    ) mp on (mu.user_type = mp.user_type )
        where
        	mu.user_id = #{userId}
        	and mu.password = #{password}
        	and mu.status = 'active'
    </select>
    
    <update id="updateToken">
        update public.m_user 
        set
        	token = #{token}
        	,expires_time = now() + interval '1 day'
        	,last_login_time = now()
        where
        	user_id = #{userId}
    </update>
    
    <select id="selectUserInfoByToken" resultType="com.wm.entity.UserInfoEntity">
       SELECT
       		user_id
            ,user_name
            ,user_type
            ,expires_time
        FROM
        	public.m_user
        where
        	token = #{token}
        	and status = 'active'
    </select>
    
    <update id="deleteToken">
    	update public.m_user 
        set
        	token = null
        	,expires_time = null
        where
        	user_id = #{userId}
    </update>
</mapper>