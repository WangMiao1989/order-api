<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wm.mapper.UserRepository">
    <select id="selectUserInfo" resultType="com.wm.entity.UserEntity">
       SELECT
            user_name
            ,user_type
            ,last_login_time
        FROM
            public.m_user
        where
        	user_id = #{userId}
        	and password = #{password}
        	and status = 'active'
    </select>
    
    <insert id="inserToken">
       Insert into public.t_auth (
       	user_id, 
       	token,
       	expires_time
       ) values ( 
       	#{userId}, 
       	#{token},
       	now() + interval '1 day'
       );
    </insert>
</mapper>