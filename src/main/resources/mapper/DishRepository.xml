<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wm.mapper.DishRepository">
    <select id="selectAllDishes" resultType="com.wm.entity.DishInfoEntity">
       SELECT
            md.id as dish_id
            , name
            , description
            , category_id
            , price
            , price * coalesce(td.rate, 1) as current_price
            , image
            , tag
            , case when tda.id is null then false else true end has_discount
            , tda.rate
            , tda.start_date
            , tda.end_date
            , md.display_flag 
        FROM
            #schema#.m_dish md
            left join #schema#.t_discount td on md.id = td.dish_id and now()::date between td.start_date and td.end_date
            left join #schema#.t_discount tda on md.id = tda.dish_id
        ORDER BY
            md.display_flag desc, md.id
    </select>
    
    <insert id="insertDish" useGeneratedKeys="true" keyProperty="dishId" parameterType="com.wm.entity.DishEntity">
    	insert into #schema#.m_dish
    		(name, description, category_id, price, image, tag, display_order, create_user, create_time, update_user, update_time)
    	values(
    		#{name}
    		, #{description}
    		, #{categoryId}
    		, #{price}
    		, #{image}
    		, #{tag}
    		, #{displayOrder}
    		, #{createUser}
	       	, #{createTime}
	       	, #{updateUser}
	       	, #{updateTime}
    	)
    </insert>
    
    <update id="updateDish" parameterType="com.wm.entity.DishEntity">
    	update #schema#.m_dish
    		set
	    		name = #{name}
	    		,description = #{description}
	    		,category_id = #{categoryId}
	    		,price = #{price}
	    		,image = #{image}
	    		,tag = #{tag}
	    		,display_order = #{displayOrder}
	    		,update_user = #{updateUser}
	    		,update_time = #{updateTime}
	    where
	    	id = #{dishId}
    </update>
    
    <delete id="deleteDish" parameterType="Long">
    	delete from #schema#.m_dish where id = #{dishId}
    </delete>
    
    <update id="updateDishDisplay" >
    	update #schema#.m_dish
    		set
	    		display_flag = #{displayFlag}
	    where
	    	id = #{dishId}
    </update>
</mapper>