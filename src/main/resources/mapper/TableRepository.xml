<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wm.mapper.TableRepository">
    <select id="selectTableInfo" resultType="com.wm.entity.TableInfoEntity">
       SELECT
            table_id
            ,table_no
            ,customer_cnt
            ,start_time
            ,end_time
        FROM
            public.t_table_info
        where
        	table_no = #{tableNo}
        	and end_time is null
        order by update_time desc
        limit 1
    </select>
    
    <select id="selectOrderList" resultType="Object">
       SELECT
            order_detail
        FROM
            public.t_order_info
        where
        	table_id = #{tableId}
    </select>
</mapper>