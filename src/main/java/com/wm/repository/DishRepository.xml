<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wm.repository.DishRepository">
    <select id="findAllDishes" resultType="com.wm.entity.DishEntity">
       SELECT
            id
            , name
            , description
            , category_id
            , price as original_price
            , price * coalesce(td.percent, 1) price
            , image
            , tags 
        FROM
            public.m_dish md
            left join t_discount td on md.id = td.dish_id and now() between td.effective_start_date and td.effective_end_date
        ORDER BY
            display_order
    </select>
</mapper>